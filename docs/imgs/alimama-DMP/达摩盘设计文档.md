```1.基础的特征包括：年龄、性别、年代、身高、体重、星座
	年龄 ：根据basesLine的数据 user—info.birthday ,加权 order_info.user_id = user_info.id 订单表1喝用户表的关联关系
		根据:
		用户行为日志:
		浏览、搜索、收藏、加购、购买，这些行为进行对虚拟年龄判断
		数据存储在日志数据中进行分流处理的action数据中，action josn块中的action_id为key 有favor_add,get_coupon,cart_add
		年龄段分为 6 类：18-24 岁、25-29 岁、30-34 岁、35-39 岁、40-49 岁、50 岁以上。
		类目偏好：base_category1.name的数据字段
		品牌偏好：根据spu_info.spu_name的字段进行ik分词炒作，再爬取各个领域热门品牌的名称，再根据lua语法,redis数据库里的品牌名称对比，对品牌字段做标记
		价格敏感度：order_info.total_amount进行计算
		时间行为：日志数据的ts进行技术
		搜索词分析:
		社交互动行为：无
		发表评论：根据业务数据的comment_info.comment_txt来判定评论数据
		分享商品：无
		设备信息：日志数据中：common键中的os的值获取
			设备分为进行分类：
			iOS 设备：使用苹果公司的 iPhone 或 iPad 等设备。
			Android 设备：使用基于 Android 系统的各种品牌手机或平板电脑。
			桌面端（PC/Mac）：通过个人电脑或 Mac 访问淘宝。
			最新系统版本：用户是否倾向于更新到操作系统的最新版本。
			高端设备：指价格较高、配置较好的设备。
	性别：
		计算逻辑：
		1. 行为权重分配
		    - 购买行为（50%）、加购/收藏行为（30%）、浏览行为（20%）。
		2. 品类分类规则：
		    - 女性相关品类（女装、美容护肤等）、男性相关品类（男装、运动鞋等）、家庭相关品类（家居用品等）。
		3. 评分计算：
		     - 计算女性得分和男性得分，差值>10%则归类为对应性别，否则为家庭用户。
		     -
	年代：根据user—info.birthday进行一个截取years然后进行分类，例如：1960到1969为六十年代
	身高：无
	体重：无
	星座:
		计算逻辑：
			1.星座匹配：根据用户生日字段的月份和日期匹配星座分界点。
			2.默认值：未填写生日则标记为“未知”。
			CASE 
		    WHEN (month = 1 AND day >= 20) OR (month = 2 AND day <= 18) THEN '水瓶座'
		    -- 其他星座类似
		    ELSE '未知'